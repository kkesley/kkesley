{
  "name": "kkesley",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "aws-appsync": "^1.8.1",
    "bulma": "^0.7.5",
    "env-cmd": "^9.0.3",
    "graphql.macro": "^1.4.2",
    "node-sass": "^4.12.0",
    "react": "^16.8.6",
    "react-apollo": "^2.5.8",
    "react-dom": "^16.8.6",
    "react-markdown": "^4.0.9",
    "react-router-dom": "^5.0.1",
    "react-scripts": "3.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build:bare": "react-scripts build",
    "build:dev": "env-cmd .env.development && yarn build:bare",
    "build:prod": "env-cmd .env.production && yarn build:bare",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@fortawesome/fontawesome-free": "^5.9.0"
  }
}
