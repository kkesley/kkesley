query Blogs($next_token: String) {
    listBlogs(next_token: $next_token) {
        items {
            blog_id
            timestamp
            title
            tags
        }
        next_token
    }
}